#server:
#  port: 9200
spring:
  application:
    name: ws-retail-itemservice
  devtools:
    livereload:
      enabled: true

  data:
    mongodb:
      uri: mongodb://${MONGO_HOST}:27017/reach-engine
      username: root
      password: rootpassword
      authentication-database: admin
      repositories:
        type: AUTO

logging:
  level:
    org:
      mongodb=DEBUG: